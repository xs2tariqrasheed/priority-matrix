{
  "name": "priority-matrix",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "add-user": "npm --prefix server run add-user --",
    "seed": "npm --prefix server run seed --",
    "dev": "concurrently -n api,web -c blue,green \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build && npm --prefix server run build",
    "start": "npm --prefix server run start",
    "typecheck": "npm --prefix server run typecheck && npm --prefix client run typecheck",
    "set-password": "npm --prefix server run set-password --",
    "reset-password": "npm --prefix server run reset-password --"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
